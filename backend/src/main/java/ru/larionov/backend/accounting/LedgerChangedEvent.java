package ru.larionov.backend.accounting;

import java.util.UUID;

/**
 * Денежная книга бота изменилась — кэшированную сводку пора выбросить.
 *
 * Инвалидация по событию, а не по таймеру: сводка обязана быть точной (это деньги
 * на экране), а пересчитывать её по расписанию значило бы либо показывать устаревшее,
 * либо грузить книгу вхолостую.
 */
public record LedgerChangedEvent(UUID botId, boolean dryRun) {
}
